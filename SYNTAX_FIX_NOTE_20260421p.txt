已修复本次报错：

错误：
SyntaxError: Failed to execute 'write' on 'Document': Invalid or unexpected token

原因：
我上一版在销毁流程里插入了一段确认文案时，把多行文本直接写进了单引号字符串，
导致浏览器解析脚本时报语法错误，整个页面脚本会提前中断。

修复：
- 将该确认文案改为合法的 \n\n 字符串
- 重新检查了页面里的 4 个 script 块，语法都已通过检查

建议：
部署这版后，访问时带一下版本参数避免钱包浏览器缓存旧文件：
zrta.asia?v=20260421p
